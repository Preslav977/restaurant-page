import {
  createHeader,
  createTabMenu,
  createHomeTab,
  createFooter,
} from './initialLoad';

createHeader('header', 'restaurant-header', 'Bulgarian Restaurant');
createTabMenu('restaurant-tab');
createHomeTab();
createFooter();
